# PostgreSQL Configuration
POSTGRES_DB=healthcheck
POSTGRES_USER=healthcheck
POSTGRES_PASSWORD=your-secure-password-here

# Application Security
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
ENVIRONMENT=production

# CORS (Update with your domain after deployment)
CORS_ORIGINS=["https://yourdomain.com","https://www.yourdomain.com"]

# SMTP Configuration (Optional - for email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourdomain.com

# Redis (Optional - only if using Redis)
# REDIS_PASSWORD=your-redis-password-here

# Droplet/Server Configuration (for GitHub Actions)
# Add these as secrets in GitHub repository settings
# DROPLET_HOST=your.server.ip.address
# DROPLET_USER=root
# DROPLET_SSH_KEY=your-private-ssh-key
